#ifndef __VOLTMETER_POWER_H__
#define __VOLTMETER_POWER_H__

/* Setup clocks. */
extern void power_setup(void);
/* Power-off the MCU. */
extern void power_off(void);

#endif /* __VOLTMETER_POWER_H__ */
